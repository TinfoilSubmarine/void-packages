if [ "${UPDATE}" = "no" ] && [ "${ACTION}" = "post" ]; then
	if [ ! -e /etc/rebased/COOKIE ]; then
		dd if=/dev/urandom bs=40 count=1 | base64 > /etc/rebased/COOKIE
	fi
	chmod 600 /etc/rebased/COOKIE
	chown _rebased:_rebased /etc/rebased/COOKIE
fi
